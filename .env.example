# JWT secret for backend. Must be at least 32 bytes (256 bits).
# Generate one with: openssl rand -base64 32
JWT_SECRET=please-change-this-to-a-real-32-byte-secret-min

# Optional: change if you customize ports
POSTGRES_PORT=5432
REDIS_PORT=6379
BACKEND_PORT=8080
FRONTEND_PORT=80
