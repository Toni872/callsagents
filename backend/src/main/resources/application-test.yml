spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/callsagents_test
    username: callsagents
    password: callsagents
  jpa:
    show-sql: false

app:
  jwt:
    secret: ${JWT_SECRET:test-secret-test-secret-test-secret-1234}
    access-token-ttl: 15s
    refresh-token-ttl: 1h
