# TEMPORAL - solo dev local
# El Postgres nativo de Windows (PostgreSQL 18) ocupa el puerto 5432 del host,
# asi que el contenedor publica en 5433. El backend dev apunta a 5433 via env.
services:
  postgres:
    ports:
      - "5433:5432"